# xhs 工作区协作约定

## Skill 蒸馏节奏（强制）

**每完成 3 个 Phase（约 20–25 个 Task）做一次"窗口经验蒸馏"** —— 把当前会话期间踩到的非平凡陷阱、形成的工程铁律提炼到当前使用的 Skill（如 `.claude/skills/idea-to-coding-plan-v2/SKILL.md`）。

### 触发条件（满足任一即蒸馏）

- ✅ 已完成 ≥ 3 个 Phase（基础节奏）
- ✅ 刚通过一个 🔴 高风险 Phase（不等节奏，立刻蒸馏）
- ✅ 单个会话内出现了 ≥ 3 条非平凡的工程决定
- ✅ 用户主动说"蒸馏"或"sync skill"

### 蒸馏产出格式

- Skill 版本号 +0.1（如 v2.1 → v2.2）
- 新增"实战陷阱"或"实战经验"章节，每条铁律包含：
  1. **铁律一句话**
  2. **踩过的坑**：当时踩坑的具体场景（来源可追溯）
  3. **正确做法**：操作层指引
- Red Flags 同步扩展（把违反新铁律的行为加进去）
- 末尾签名 `*<skill-name> vX.Y · 蒸馏自 <来源会话/项目> · YYYY-MM-DD*`

### 蒸馏反模式（避免）

- ❌ 每个 Task 完成都蒸馏（噪声大，淹没真正的铁律）
- ❌ 每个 Phase 都蒸馏（密度不够，无新发现也强写）
- ❌ 蒸馏只总结"做了什么"而不抽炼"为什么以前这条没在 Skill 里"
- ❌ 不更新 Red Flags 只加章节（半成品蒸馏）
- ❌ 蒸馏后不同步到 GitHub 仓库副本（只更新本地）

### 蒸馏的"非平凡"判定

> 一条经验值得蒸馏，当且仅当：**如果这条经验当初就在 Skill 里，能让 ≥ 1 个具体步骤的踩坑/返工被避免。**

仅"我学了点什么"不算 — 必须是行动层（"应该怎么写代码 / 怎么组织文件 / 怎么测试"）能落到 Skill 文本的具体规则。

---

## 其他通用约定

- 项目脚手架前必须做"参考成熟项目盘点"（见 `idea-to-coding-plan-v2` Skill §参考成熟项目原则）
- 浏览器扩展项目 L3 §1.2 必须画"消息网格图"（dashboard ↔ SW ↔ content script ↔ MAIN world）
- Phase 结束必须跑 typecheck + vitest + build 三连质量门
- 不在 main / master 上启动实施，必须 worktree / feature branch
- 所有 Task 严守 TDD 铁律：先写失败测试 → 验证失败 → 写最小实现 → 验证通过 → 提交

---

*xhs 工作区 CLAUDE.md · 2026-05-09 创建*
